input {
  http {
    host => "0.0.0.0"
    port => "2000"
  }
}

output {
  stdout { codec => rubydebug }
  rabbitmq{
	exchange => "ls-rmq"
	exchange_type => "direct"
    host => "localhost"
    durable => true
	persistent => true
    key => "lsbanana"
	workers => 4
  }
}